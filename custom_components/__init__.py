"""Make custom_components a proper package for static analysis.

This file is intentionally empty; its presence prevents mypy from
reporting the same module under both `smart_charger` and
`custom_components.smart_charger` when running static checks.
"""
